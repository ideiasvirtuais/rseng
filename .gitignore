# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# FTP deploy credentials (local only)
.env.ftp

# Credenciais de deploy FTP por ambiente
.env.ftp
.env.ftp.staging
.env.ftp.production

# Env local (nunca commitar segredos locais)
.env

# Config local da ferramenta (opencode) — não é código do projeto
config.json

# Projeto usa bun.lock — ignora lockfile gerado pelo npm
package-lock.json

# Assets vendorados gerados localmente (regeneráveis via scripts/vendor-cdn-assets.mjs)
public/__l5e/

# CodeIn auto-ignore
.git
dist-electron
dist-release
build
.next
.vite
.turbo
.cache
coverage
